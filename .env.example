# Postgres (dev)
DATABASE_URL="postgresql://psi:psi@localhost:5432/clinica_ia"

# Postgres (test) — used by integration tests
DATABASE_URL_TEST="postgresql://psi:psi@localhost:5433/clinica_ia_test"

# Auth.js — required
AUTH_SECRET=""                                                    # openssl rand -base64 32  (or node -e "console.log(require('crypto').randomBytes(32).toString('base64'))")
AUTH_URL="http://localhost:3000"
AUTH_TRUST_HOST="true"

# Email (Resend) — optional; in dev, links are logged to console
RESEND_API_KEY=""
EMAIL_FROM="Clínica IA <nao-responda@clinicaia.local>"

# Seed — first owner of the clinic
INITIAL_USER_EMAIL="marina@clinicaia.local"
INITIAL_USER_PASSWORD=""                                          # set before running npm run db:seed
INITIAL_USER_NAME="Dra. Marina Azevedo"
INITIAL_USER_CRP="CRP 06/123456"
